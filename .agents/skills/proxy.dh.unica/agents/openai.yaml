interface:
  display_name: "Proxy.dh.unica"
  short_description: "Repo-specific patterns and workflows for proxy.dh.unica"
  default_prompt: "Use the proxy.dh.unica repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true